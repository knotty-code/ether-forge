---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: edgerouterstates.sandy.eda.local
spec:
  group: sandy.eda.local
  names:
    kind: EdgeRouterState
    listKind: EdgeRouterStateList
    plural: edgerouterstates
    singular: edgerouterstate
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: EdgeRouterState is the Schema for the edgerouterstates API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EdgeRouterStateSpec defines the desired state of EdgeRouterState
            properties:
              nodes:
                description: List of TopoNodes this login banner has been applied
                  to
                items:
                  type: string
                type: array
            type: object
          status:
            description: EdgeRouterStateStatus defines the observed state of EdgeRouterState
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
