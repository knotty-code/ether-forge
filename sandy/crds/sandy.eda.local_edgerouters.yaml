---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: edgerouters.sandy.eda.local
spec:
  group: sandy.eda.local
  names:
    kind: EdgeRouter
    listKind: EdgeRouterList
    plural: edgerouters
    singular: edgerouter
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: EdgeRouter is the Schema for the edgerouters API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EdgeRouterSpec defines the desired state of EdgeRouter
            properties:
              loginBanner:
                description: This is the login banner displayed before a user has
                  logged into the Node.
                type: string
              nodeSelector:
                description: Label selector to select nodes on which to configure
                  the banners.
                items:
                  type: string
                type: array
              nodes:
                description: List of nodes on which to configure the banners.
                items:
                  type: string
                type: array
            type: object
          status:
            description: EdgeRouterStatus defines the observed state of EdgeRouter
            properties:
              nodes:
                description: List of nodes this banner has been applied to
                items:
                  type: string
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
