cells:
  spine_1:
    label:
      text: "Spine 1"          # ← the actual display text goes here as a string
      valueFormat: ""          # optional: custom format string (e.g. "{value:.1f} bps")
      decimalPoints: 1         # optional: number of decimal places
      # other label options if supported (e.g. position, fontSize, etc. – check examples)
    thresholds:
      - value: 0
        color: green
      - value: 50
        color: yellow
      - value: 100
        color: red
    dataRef: test-value        # your test or real query alias