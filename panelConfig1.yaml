cells:
  spine_1:
    dataRef: "test-data-small-sin"      # ← uses built-in test data (sine wave, small range)
    label:
      separator: "cr"                   # "cr" = carriage return (new line)
      units: "units"                    # just for visibility
      decimalPoints: 0
    labelColor:
      gradientMode: "hue"
      thresholds:
        - {color: "green", level: 0}
        - {color: "orange", level: 50}
        - {color: "red", level: 100}