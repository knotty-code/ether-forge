---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: linkapps.plumber.eda.local
spec:
  group: plumber.eda.local
  names:
    kind: LinkApp
    listKind: LinkAppList
    plural: linkapps
    singular: linkapp
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LinkApp is the Schema for the linkapps API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              LinkAppSpec defines the desired state of LinkApp
              This is the rig that tells ol' Jack which two ends to hook up.
              You give me the local and remote interfaces, and I make 'em shake hands like thunder rollin' through Chinatown.
              It's all in the reflexes.
            properties:
              local:
                description: Pick your home base — the local interface we're startin'
                  from. Better be a real one, or ol' Jack's comin' after ya with the
                  Pork Chop Express.
                type: string
              remote:
                description: The other side of the deal — the remote interface we
                  gotta connect to. Point me right, and I'll bridge 'em faster than
                  you can say "it's all in the reflexes."
                type: string
            type: object
          status:
            description: LinkAppStatus defines the observed state of LinkApp
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
