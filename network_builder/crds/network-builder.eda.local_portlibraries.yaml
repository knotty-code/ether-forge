---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: portlibraries.network-builder.eda.local
spec:
  group: network-builder.eda.local
  names:
    kind: PortLibrary
    listKind: PortLibraryList
    plural: portlibraries
    singular: portlibrary
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PortLibrary is the Schema for the portlibraries API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PortLibrarySpec defines the desired state of PortLibrary
            properties:
              nodeSelector:
                description: Label selector to select nodes on which to configure
                  the banners.
                items:
                  type: string
                type: array
              nodes:
                description: List of nodes on which to configure the banners.
                items:
                  type: string
                type: array
              port:
                type: string
            type: object
          status:
            description: PortLibraryStatus defines the observed state of PortLibrary
            properties:
              adminState:
                type: string
              opstate:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
