---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: subnetgenies.network-builder.eda.local
spec:
  group: network-builder.eda.local
  names:
    kind: SubnetGenie
    listKind: SubnetGenieList
    plural: subnetgenies
    singular: subnetgenie
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SubnetGenie is the Schema for the subnetgenies API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SubnetGenieSpec defines the desired state of SubnetGenie
            properties:
              subnet:
                default: '''10.0.0.0/29'''
                description: IPv4 subnet to allocate subnets from, e.g. 10.1.0.0/16
                type: string
              subnetLength:
                default: 30
                description: The size of the subnets to be allocated from within the
                  parent subnet, e.g. 29 (which could allocate 10.1.0.8/29, for example).
                type: integer
            type: object
          status:
            description: SubnetGenieStatus defines the observed state of SubnetGenie
            properties:
              nodes:
                description: List of nodes this banner has been applied to
                items:
                  type: string
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
