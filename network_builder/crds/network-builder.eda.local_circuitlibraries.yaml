---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: circuitlibraries.network-builder.eda.local
spec:
  group: network-builder.eda.local
  names:
    kind: CircuitLibrary
    listKind: CircuitLibraryList
    plural: circuitlibraries
    singular: circuitlibrary
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CircuitLibrary is the Schema for the circuitlibraries API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CircuitLibrarySpec defines the desired state of CircuitLibrary
            properties:
              nodes:
                description: List of nodes on which to configure the banners.
                items:
                  type: string
                type: array
              portA:
                description: Select interface from Port Library.
                items:
                  type: string
                type: array
              portB:
                description: Select interface from Port Library.
                items:
                  type: string
                type: array
              subnets:
                items:
                  type: string
                type: array
              supernet:
                items:
                  type: string
                type: array
            type: object
          status:
            description: CircuitLibraryStatus defines the observed state of CircuitLibrary
            properties:
              nodes:
                description: List of nodes this banner has been applied to
                items:
                  type: string
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
